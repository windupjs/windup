{
  "$comment": "windup account → ENV-name mapping. No secret values here — commit this file; values live in .env.local (gitignored) or CI secrets with these names.",
  "accounts": {
    "default": {
      "password": "ENV:WINDUP_DEFAULT_PASSWORD"
    }
  }
}
