{
  "name": "windup-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "npm -w packages/windup run typecheck",
    "test": "npm -w packages/windup test",
    "build": "npm -w packages/windup run build"
  },
  "devDependencies": {
    "windupjs": "^0.3.0"
  },
  "version": "0.0.1"
}
